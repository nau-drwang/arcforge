{
  "name": "arcforge-pages-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "pages:build": "node scripts/build.mjs",
    "build": "npm run pages:build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
